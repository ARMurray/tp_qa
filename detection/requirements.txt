# =============================================================================
# wastewater_infrastructure_detection — main env (yolo_env)
# Extraction (01, 02), dataset prep (03), training (04), inference (05).
# Keep SEPARATE from the Label Studio env (see requirements-labelstudio.txt).
# =============================================================================

# --- PyTorch: NOT pip-pinnable here (Blackwell GPU needs the cu128 nightly) ---
# Install FIRST, before the packages below:
#   pip install --pre torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/nightly/cu128
# -----------------------------------------------------------------------------

ultralytics>=8.2.0

# Geospatial
geopandas>=0.14
shapely>=2.0
rasterio>=1.3
pyarrow>=15.0
pyproj>=3.6
pygris>=0.1.6          # Census counties download (01); or set COUNTIES_GPKG

# Imaging / HTTP / data
Pillow>=10.0
requests>=2.31
numpy>=1.26
pandas>=2.0
